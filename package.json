{
  "name": "claude-cognis",
  "version": "1.0.0",
  "description": "Persistent memory across Claude Code sessions using Cognis",
  "type": "commonjs",
  "engines": {
    "node": ">= 18.0.0"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "clean": "rm -rf plugin/scripts",
    "lint": "biome check src/ scripts/",
    "lint:fix": "biome check --write src/ scripts/",
    "format": "biome format --write src/ scripts/",
    "test:live": "node tests/test-cognis-live.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "@biomejs/biome": "^1.9.0"
  },
  "license": "MIT"
}
